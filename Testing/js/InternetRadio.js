/* jshint -W138 */
/* TODO Complete JSDocs */
/* TODO Implement Functions */
class InternetRadio {

	/**
	 * Returns all internet radio stations. Takes no extra parameters.
	 * @param   {object} auth Authenticate Object.
	 * @returns {object} Returns a <subsonic-response> element with a nested <internetRadioStations> element on success.
	 */
	getInternetRadioStations(auth) {
		var result;
		return result;
	}
}
